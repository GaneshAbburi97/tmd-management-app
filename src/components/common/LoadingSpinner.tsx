import React from 'react';
import { ActivityIndicator, View } from 'react-native';

const LoadingSpinner = () => <View style={{ padding: 24 }}><ActivityIndicator /></View>;

export default LoadingSpinner;
